# ▶ RESUME HERE — Universal Location Methodology test runs

**Rewritten again 2026-08-31, after Cape Adare Runs 7 (cold) and 8 (warm), and after an ordering defect was
found and fixed.** **The next run is Run 9. Its subject is LOCKED by the developer: JANBOGO. It is also
INSTRUMENTED — see §2 and §2b, which are the two sections that differ most from every prior handoff.**
**Read this file first. It is written for a session with no memory of the previous work, which is the entire
point.**

> ## ⚠ Run 9 has a SECOND job that no prior run had.
>
> Every prior run had one job: produce a location. **Run 9 produces a location *and* carries an
> instrumentation task** — a watch for a specific, named class of defect that was fixed on 2026-08-31 but is
> **not known to be fully mapped.** The watch is described in **§2b**. It is not optional, it is not a
> footnote, and **a Run 9 that produces a beautiful Janbogo and no collision log has done half the job.**

**What changed with Run 7, briefly:** Cape Adare (highest TBD-density of all 35 outer cities, zero Hwy 37
connection) was run complete — eleven phases, sixteen gates, Review Panel; the Zodiac Lens deliberately
deferred to a follow-up pass. **Two real contamination events were self-caught inside `Specs/Cape_Adare.md`
itself** — a file-type every prior run had trusted by default — and fixed in the same session; **read
`05_The_Input_Contract.md` §6.1d before trusting any `Specs/` file's "Character," "Culture," or "Significance"
section on a future run.** The neutral-frame rule (`01` §4.1) passed its hardest test yet on this run — Cape
Adare's own canon foregrounds its post-war Destroyed status unusually prominently, and the pass held the
living, pre-war frame throughout regardless. **Full writeup:** `Test_Runs/2026-08-31_CapeAdare_Run7_Cold/` (15
files), plus a concrete follow-up plan for closing that run's own eleven REQUESTED items in its own
`14_Step9_Record_and_Step10_Readiness.md`.

**What changed with Run 6, briefly, since it's the newest thing this file assumes you know:** Highway 37 was
the first Corridor-type location run under this methodology, and the first genuinely thin one — no completed
culture pass existed for it before the run, unlike Zhongshan and Sinheung, which both turned out to be
best-case configurations despite being chosen as "thin" candidates. **Full writeup:**
`Test_Runs/2026-08-31_Highway37_Run6_Cold/` (17 files). **One methodology change came out of it, binding on
every run from here on**: `01_Frame_Typology_and_Inheritance.md` §4.1, "THE DEFAULT FRAME IS NEUTRAL" — absent
a specific reason otherwise, a pass defaults to the Second Interwar Period baseline (before the Long Night War,
before Amundsen Tower's destruction), not to the post-war present. **Read that section before declaring any
frame for Run 9** — an early Run 6 draft defaulted to post-war without being asked to, and the correction is
now a standing rule, not a one-off fix.

**Your task, restated: run Run 9 on JANBOGO — cold, complete, and INSTRUMENTED (§2b) — against the current
methodology, which now includes the Zodiac Lens technique family (§5a below), the neutral-frame default (§4.1
of `01`), the DRAFT/CLOSE ordering rule (`03` §0.4), the ratification axis (`05` §6.3), and everything Runs 6,
7 and 8 learned.** The developer's own framing after Sinheung Run 5, still the governing instruction: *"I believe I
may say that the methodology appears to be optimized. Still, we should do another few test runs just to be
sure."* **Read that as skepticism to satisfy, not as confirmation to repeat.** Every run so far (Zhongshan ×2,
Sinheung, Highway 37) was run by the same author who built the instrument — a run that only ever validates
itself is not evidence (`04` Part IV). **Plan on several runs this cycle, not one**, and watch specifically for
whether the apparent optimization holds on a location chosen because it looks least likely to conform.

---

> # ⚠ THERE IS NO TIME CONSTRAINT. NONE.
>
> **Developer instruction, stated directly and in these terms:**
>
> > *"I don't care how long it takes. If it takes 72 hours to go through the process of creating a solid,
> > repeatable, reusable methodology of establishing one location and that's it, then I'm perfectly fine with
> > it taking 72 hours. I'm not in any hurry. I don't want you to just 'runny-run' through it 'quickly'; what I
> > want is for you to get it **right**."*
>
> **Restated in full rather than cross-referenced to LAW 0, for the same reason LAW 0 is stated in full in the
> runbook: a procedure that cites its governing law instead of stating it will be run without it.**
>
> **A handoff is compact by nature, and compactness implies a haste that was never intended.** This is a
> checklist of *what to do*, **not a suggestion about how fast.** If a phase needs four hours of research,
> spend four hours. **Completion is not the goal; a place somebody could live in is the goal.**
>
> **The specific failure this guards against:** the gates can confirm a pass is not *wrong*. **None of them can
> tell you it is thin.**
>
> **Restated again, 2026-08-31, mid-Run-5, in the developer's own words:** *"Refining, fine-tuning, and
> perfecting this methodology is of absolute top-priority and importance to me. If it takes a month to get it
> right, then we spend a month testing and refining it."* **A long runtime is not a signal to wrap up. It is
> the expected shape of this work.**
>
> **Restated a third time, 2026-08-31, after Sinheung Run 5 and the Zodiac Lens family it produced:** *"I
> believe I may say that the methodology appears to be optimized. Still, we should do another few test
> runs just to be sure."* **The instruction is to confirm, not to assume.** A session that treats "several
> more runs" as a formality has misread this line.

---

# ⚠ THE RECORDING LAW — not optional, and it is the actual deliverable

**Developer instruction, given twice in one session, 2026-08-30. Binding on every pass since.**

> ## RECORD EVERY FINDING IN THE OBSERVATIONS FILE — NOT JUST THE THINGS THAT WORKED.
>
> **"Finding" does not mean "successful technique."** The developer's own enumeration:
> **ways of achieving results · snags · problems · unintended blockages · etc., etc., etc.**
>
> In practice: techniques that worked and *why* · anything that slowed you down, however small · anything the
> methodology got wrong, could not answer, or answered badly · **any step that could not be run at all, and
> what stopped it** · **contradictions between two instructions in this methodology** · dead ends and where
> they died · **killed findings — an attractive result destroyed by its own evidence, the most valuable entries
> in the file** · **self-corrections, written as *what you believed · why it was wrong · what changed it*** ·
> **environmental and tooling obstacles** — a hook, a stale index, a `CLAUDE.md` rule conflicting with a
> methodology rule · and anything merely **unclear**, even where you guessed right.

**Three rules about how, each already violated at least once:**

1. **Write it when it happens.** Precision decays.
2. **Log the failure even when you routed around it.** **Solving a problem privately is how a methodology stays
   broken.**
3. **Never compress a negative result into a positive one.**

**The file: `Test_Runs/OBSERVATIONS_and_Methodology_Findings.md`** *(top level — deliberately outside every run
folder, so no run's own quarantine can block writing to it)*.
**Number entries `M-n` continuously — check the actual file for the last used number before starting; do not
trust a number stated anywhere else, including in this file, which goes stale the moment another entry is
appended.** *(The last stale-number instruction here caused exactly this mistake once already — see M-22.)*

> **The location is the whetstone; the methodology is the deliverable. A run that produces a beautiful location
> and a thin observations log has failed at the thing it was for.**

---

# 1. What this run is, and what it is NOT

> ## ⭐ Run 9 tests the opposite property from Run 7, on purpose.
>
> **Run 7 (Cape Adare) settled the thin-input question** — and its answer was that the instrument's ceiling is
> set by *admissible input*, not by the instrument. That gap now has its own dedicated system, the **Canon Gap
> Resolution Method** (`Worldspace/Canon_Gap_Resolution_Method/`), built specifically to acquire missing
> canon; it is a separate, interacting system and **not** something this run re-derives.
>
> **Run 9 asks a question no prior run could:** does the spine hold up under a *rich* location, where all
> eleven phases fire with real content simultaneously? **Thin locations cannot answer it, because most phases
> return null and nothing collides.** See §2b — this is the run's whole second purpose.

**What "cold" means here, restated precisely:** no completed culture pass on the chosen location has been
opened by this session before its own findings are written. **The quarantine for this run is not a pre-built
list** — unlike a same-location re-run, there is no prior pass on this specific location to quarantine. The
quarantine work is: (a) confirm no completed culture material for the chosen location has already leaked into
this session's context via auto-loaded memory or an earlier conversation turn, and (b) apply the general
admissibility rule (`05` §6.1) to whatever canon files exist for the location, fresh, by rule rather than by
recall — see §3 below.

---

# 2. The location is NOT a choice this time. Run 9 is JANBOGO.

**Developer-locked, 2026-08-31. Do not re-open this, and do not substitute a "better" candidate.** The
selection criterion for Run 9 **deliberately inverts** every prior run's, and the inversion is the point:

> **Prior runs chose THIN locations, to test whether the instrument works on poor input. Run 9 chooses the
> RICHEST unrun location, to test something different: whether the instrument's own ORDERING holds when many
> phases fire at once with real content.** A thin location suppresses the defect this run is hunting, because
> collisions only occur where a later phase has enough material for an earlier check to collide with.

**Janbogo:** subnet capital of the Janbogo Subnet, 330 lines of local culture material (the largest of any
never-run city), a 200-line spec, plus subnet-level relationship canon. Being a capital, **Phase 5 (Relation)
and Phase 7 (Order) both carry real weight** rather than returning thin.

> ## ⚠ BEFORE YOU COUNT JANBOGO'S INPUT: eleven of its files are DEMOTED, not admissible.
>
> **`Background-Lore/Cities/Janbogo_Subnet/Janbogo/Course_of_Events/` — eleven files, ~1,836 lines — plus
> `Janbogo_Course_of_Events_Suggestions.md`, are NOT CONFIRMED CANON.** Developer, directly: *"those vignettes
> still need to be double-checked. I haven't determined which ones are canon."*
>
> **Read `05_The_Input_Contract.md` §6.3 before touching them.** They pass the circularity rule and fail the
> ratification one. **They are DEMOTED, not quarantined** — readable as prompts, exactly as a real-world
> inspiration is readable, but they **cannot ground a finding, settle a fact, or be cited as canon.** A finding
> resting only on them is **REQUESTED**, not PRODUCED.
>
> **This is not Janbogo-specific.** Every city has a folder of this kind, and the ratification decision across
> all 35 is open. **Run the §7 pre-flight ratification block on every input, not just this one.**

**So Janbogo's actual admissible input is smaller than its file count suggests — and that is fine.** It is
still the richest unrun subject available. **Do not pad the pass with demoted material to make it look
fuller.**

**Still required, exactly as for every prior run:** build the quarantine per §3 (the 330-line culture file is
this run's withheld comparison target, opened only at Gate 6), and state the typicality declaration for the
subject.

---

# 2b. ⚠ THE INSTRUMENTATION TASK — Run 9's second job

**Read `03_The_Phase_Spine.md` §0.4 in full before Phase 0. This section assumes it.**

**What was found and fixed on 2026-08-31 (M-61):** the spine had silently merged **DRAFT order** (phases are
*written* 0 → 10) with **CLOSE order** (a few checks are only meaningful against a *complete* file). Because
the second was never named, **running sessions invented forward dependencies to describe the collision** —
writing things like *"this section will be re-checked once Phases 5–10 are written"* inside Phase 4. The
methodology never said that. **The sessions generated it themselves**, which is why searching the method files
for the defect returned clean for as long as it did.

**§0.4 now carries a four-row close-pass docket. It is explicitly NOT known to be complete** — it was built
from two runs, both on locations thinner than Janbogo.

### Your instrumentation task, stated concretely

1. **Keep a running log file, `NN_Ordering_Collision_Log.md`, from Phase 0 onward.** Not written at the end
   from memory — **written as it happens.** A collision reconstructed afterward is the recording law's known
   failure mode (see the recording law section above).
2. **Log every point where a check cannot be completed in the phase that owns it**, whether or not it is on
   the docket. For each: **which item, which phase owns it, what it needs, which phase supplies that, and
   where you closed it.**
3. **Flag any collision NOT on the four-row docket loudly.** Those are the finding. The four known rows are
   the control, not the result.
4. **Do NOT write a forward dependency to resolve one.** That is the defect. **A phase that defers a
   complete-file check is COMPLETE, not BLOCKED** — close the phase, docket the check, move on. If you find
   yourself typing *"pending Phase Y"* about a later phase, stop and consult §0.4.
5. **Record the negative result too.** If the fix holds and no new collision appears, **say so explicitly and
   say what you watched for.** A clean instrumented run is a real result; an unstated one is worthless.

> **⚠ The honesty trap specific to this run.** You will read §0.4 before you start, so you will be primed not
> to generate the bug. **That is intended — it is the fix working.** But it also means a clean log is weak
> evidence unless you state clearly *what you watched for and where you nearly slipped.* **Record near-misses:
> any moment you wanted to defer something and had to check the docket to decide how.** Those are the most
> informative lines you will write.

**Guidance below is for narrowing *within* the developer's constraint, not for overriding it — retained from
the Run 7 handoff because the reasoning still applies to any future run whose subject is open:**

1. **Prefer a city with thin, sparse, or largely absent existing canon** over one with a rich pre-existing
   culture file — this is the developer's own explicit framing for Run 7, not merely this file's usual
   preference. A city where most of the eight generators (`02` §2) return null or weak is the actual target.
2. **Zhongshan and Sinheung have both now been fully run and are not eligible.** **Shirayuki is NOT
   disqualified by the highway-connection rule** (it sits on Hwy 110, not Hwy 37) and remains genuinely viable,
   but note carefully: Sinheung was chosen on a "thin canon" assumption that turned out wrong (Run 5's own
   finding), and Shirayuki's own canon depth has not been independently verified as thin — check before
   assuming it satisfies the developer's specific "under-developed" ask for Run 7, rather than treating
   "never run" and "under-developed" as the same property. **If Shirayuki's canon turns out comparably rich,
   prefer a genuinely thinner city instead**, even one less well-known. **A session choosing Shirayuki must
   still verify the inbound memory scan (§4)** — the banners from M-21's fix should cover it, but verify rather
   than assume, and check specifically for the M-32 fix's own banded entry
   (`project_refugee_affinity_verification_pass.md`), which also carries a leaked Signy claim unrelated to
   Shirayuki but sits in the same file.
3. **Type/Band is not the axis for this run** — Run 6 already tested an unusual Type (Corridor); Run 7 is
   specifically a Settlement/city per the developer's own instruction, testing thinness within the familiar
   Type rather than testing a new Type. Do not substitute a Corridor, Natural feature, or other unusual Type for
   Run 7 on the theory that it satisfies the same general goal — it does not satisfy this run's specific brief.
4. **If genuinely undecided between two or three candidate cities**, state the candidates and the tradeoff
   (canon depth, highway independence confirmed for each), and ask the developer to choose rather than picking
   silently — this is exactly the kind of decision `01` §5.3a's "configuration: typical/exceptional" declaration
   exists to make visible, and a silent choice here is a silent bias the rest of the pass cannot correct for.

**State the typicality declaration for the choice itself**, per `00_RUNBOOK.md`: is the chosen location
typical or exceptional for its type, and if exceptional, which findings will depend on that.

---

# 3. Building this run's own quarantine — by rule, not by copying a prior list

**No pre-built do-not-open list exists for this run**, because the subject location has not been chosen yet.
Once chosen, build the list fresh, using `05` §6.1's content split, applied section by section — **not by
recall, and not by copying the shape of a prior run's list**, which was built for a different location's
specific files.

## 3a. FIRST: run the INBOUND readiness check on yourself

**`00_RUNBOOK.md` Step 10 is a standing step, and it has two directions.** The outbound half runs at the *end*
of a pass, by whoever hands off. **The inbound half is yours, and it runs now, before you read anything else
about the chosen location:**

1. **Scan your auto-loaded memory for the chosen location's name**, and for any adjacent location names (a
   subnet, a cluster, a parent). Every hit must be **attribute-only** or **banner-warned**. **If an un-banded
   entry states the location's character, temperament, personality triple, or a signature phrase — stop
   reading it and band it before continuing.** *(This is the one vector no do-not-open list can intercept —
   see §5 below.)*
2. **Check `06_Worked_Example_Provenance.md`** for whether the chosen location carries any manifested worked
   examples in `00`–`05`/`00f` — unlikely for a genuinely new subject, but check by name rather than assuming.
3. **Open the header of every file you are about to treat as admissible**, and check its own cited sources.
   **A file that cites a withheld or conclusion-bearing document is downstream of it, whatever its filename
   says.**
4. **Confirm the location's own canon files actually exist where the registry (`00_RUNBOOK.md` §B–D) says they
   should**, rather than assuming a standard file layout — some location types will not have a `Local_Cultures`
   sheet, an Enneagram read, or a Megasheet folder at all, and that absence is itself an input (a REQUESTED-
   input case, per `05` §5), not a gap to paper over.

**Record what the inbound check finds, including "nothing."** A clean check is evidence; an unrun one is not.

## 3b. The general admissibility rule, applied fresh to whatever the chosen location has

Per `05` §6.1's operational split:

- **ADMISSIBLE:** physical/environmental facts, founding mechanism/date/circumstance, function/industry,
  network position/routes, census/composition/population-change, dated events, a research log (if one exists
  for this location — it holds attributes, never conclusions, per `05` §6.1).
- **INADMISSIBLE:** any prior culture-pass conclusion about the location — character, temperament, capability
  profile, shape, axis, or differentiation claim — however it is phrased and whatever file it lives in.
- **⚠ Admissibility is a property of content, never of filename or folder** (`05` §6.1a). **A file is
  admissible only if every section of it is.** Check headers for cited sources before trusting a file's name.
- **⚠ Row-level mixing** (one admissible column, one inadmissible column, in the same table — e.g., a symbol
  assignment's *members* vs. its derived *rationale*) needs a column-anchored search pattern, not a bare-name
  search, or the inadmissible field leaks through (`05` §6.1a's own worked note).

## 3c. Navigate by path, never by query

**This repo enforces a `PreToolUse` hook requiring `graphify query` before file reads, and `CLAUDE.md` repeats
it.** **A graph query naming the chosen location returns extracts from whatever culture-pass conclusions exist
for it** — retrieval indexes content, and quarantine is a property of provenance, which a corpus-wide index
cannot honor. **Use `find`/`ls` to locate files and direct reads to what is confirmed admissible. Declare this
violation of the tooling convention deliberately, in the observations log** — the circularity rule outranks a
search convention.

## 3d. When you must EDIT a file you may not READ

**It will happen** — a developer ruling, a typo, a canon fix landing on an inadmissible file. **Do not read it,
and do not defer it.**

> **Locate by `grep` (shows one line) · patch by exact-match script with `assert count == 1` (shows none) ·
> verify by `grep` (shows one line).** Three narrow windows instead of one open door. Where the match itself
> would expose inadmissible content on the same line (a mixed table row), anchor the search pattern to only
> the fields you need — see §3b.

## 3e. If a per-location research log exists

**Check `Cities/Research_Logs/` (or the equivalent folder for the location's own type) for a dedicated log
before assuming none exists.** A research log records exact search strings, sources, a fact→finding table, what
was withheld vs. omitted, divergences from source, and open threads — **it holds attributes, never
conclusions, so it stays admissible even to a cold run**, and open threads are frequently the highest-value
unpursued material available. If none exists for the chosen location, create one at Step 3 of the task below,
per `00_RUNBOOK.md` §3.7.

---

# 4. AUTO-LOADED MEMORY reaches you before any of this does

**Every rule in §3 assumes you *choose* to open a file. Memory does not work that way — it is pushed into the
session, and no do-not-open list can intercept it.**

**Before doing anything else, scan your memory directory for the chosen location.** Every hit must be either
**attribute-only** *(founding, dates, names, census, corrections, open questions)* or **carry a contamination
banner.** **If you find an un-banded entry stating the location's character, temperament, personality triple,
or a signature phrase — stop reading it, and add a banner before you continue.**

**Standing rules, established after a real contamination event (M-21):**

> 1. **A memory entry about a location records ATTRIBUTES and STATUS, never culture-pass conclusions.**
>    Founding mechanism, dates, names, census, corrections made, what is still open — yes.
>    *"Its character is X," "its temperament reads as Y,"* a personality triple, a signature phrase — **no.**
>    **Point to where the conclusion lives instead.**
> 2. **A technique memory records the technique, not its results.** The results have a home in the repo, where
>    a quarantine can actually govern reading them.
> 3. **Where an entry genuinely needs the conclusion** — a rule taught by worked example, a bug-check log —
>    **it carries a contamination banner immediately after its frontmatter.**
> 4. **Before any cold run, scan memory for the subject location** and check that every hit is either
>    attribute-only or banner-warned. **It is a two-minute check and nothing else performs it.** This is
>    `00_RUNBOOK.md` Step 10.1, item 1 — a standing step, not a suggestion.

---

# 5. The exact task

1. **Choose the location** (§2), and state the choice and its typicality declaration before anything else.
2. **Create `Test_Runs/[Date]_[Location]_Run1_Cold/`** — name it for the actual location and this being its
   first run, not by copying a prior run's folder-naming pattern. **Confirm the folder does not already exist**
   before creating it (`00_RUNBOOK.md` Step 10.2).
3. **Check `06_Worked_Example_Provenance.md` first**, then read `00_RUNBOOK.md` in full, then `01`–`05`. For
   this location, none of the manifested examples in those files should be about it (§2 above rules that out)
   — but check by name rather than assuming, and if any are, skip them per that file's own rule.
4. Read the disciplines: `00b` general population · `00d` shadow proportion · `00f` review panel *(note its
   six-disposition set: accepted · noted · rejected · refereed · unmet · declined)* ·
   `../Cultural_Synthesis_Techniques.md` · `../Real-World_Basis_Extrapolation_Method.md`.
5. **Run the `05` §7 pre-flight**, including Configuration, provenance, and the typicality declaration.
6. **Read canon in the Step 0.4 triage order** — specs → symbols → census *(and change across both, if
   multiple snapshots exist)* → founding → physical facts → **culture files never**, per §3b above.
7. **Write Phases 0–10.** Record nulls **with reasons**, distinguishing *covered in substance, absent in form*
   from *absent and unexplained*. **Where a phase is partially written and partially deferred, name which
   specific claims are grounded and which are not — a phase-level null flag does not cover every individual
   claim inside it** (`03` §0.2 item 4, added after a real case where it did not).
7a. **⚠ Added 2026-08-31 — run the Zodiac Lens technique family, feeding Phase 9/10.** Per `../Cultural_
   Synthesis_Techniques.md`'s Technique — The Zodiac Lens: (i) the base twelve-sign run, against this
   location's own established character, never against Concordia's own application of the signs; (ii) the
   Elemental/Planetary Cross-Check extension (216 prompts: 12 signs × 8 Robot Elementals × 10 Robot Planetary
   Symbols) — **recommended as twelve parallel subagents, one per sign**, plus a coordinating-session-only
   final cross-sign synthesis pass; (iii) the per-HIT contradiction check on every kept result from both. **The
   stopping-criterion rule (step 6 of that technique) is binding at every layer** — a result count, at any
   number, is never itself evidence of a real search; state why the search stopped where it did. **Reserve real
   time for this** — Sinheung's own first full run of this family took a comparable share of that run's total
   effort to the eleven phases and sixteen gates combined, and produced results (including one that closed a
   real Review Panel gap) that no other part of the methodology reached.
8. **Run all sixteen gates** — 0–11 plus **C · F · I · P · G**. **Paste raw scan output; never summarize.**
   **Verify every instrument before trusting any figure, zero or plausible.**
9. **Convene the Review Panel** (Step 8 of `00_RUNBOOK.md` — the Review Panel specifically, and only the
   Review Panel; do not reuse "Step 8" to mean anything else, per M-31) — six Flat Archetypes, the mandatory
   Passer-Through and Neighbor, the Lover faculty's question every time, and the six-disposition set.
10. **Only then** open any withheld/inadmissible files for this location (there may be none, if this is
    genuinely the location's first pass), run Gate 6 (deferred to this point per `04`'s own note), and record
    matches and mismatches. **Before recording any mismatch as a kill, run the both-are-true test** (`02` §5.3,
    `04`'s Gate 6 note) — a contradiction is very often a scale mismatch (public/private, mainstream/
    counterculture, generational, legal-vs-narrative), not a wrongness, and should be checked before being
    discarded.
11. **Write up what the instrument did**, and **append every finding to the shared observations file** — see
    the Recording Law above.
12. **Create or append to this location's own research log**, per §3e — append, never overwrite, if one
    already exists.
13. **Run the OUTBOUND half of `00_RUNBOOK.md` Step 10** before declaring the pass complete or handing off:
    update `06_Worked_Example_Provenance.md` with any new worked examples this pass adds to the methodology
    (in a separate per-location archive file, cross-referenced by pointer — not written inline into the rule
    files, per M-30); confirm findings were **implemented**, not merely recorded, in the same commit; state
    plainly what this run can and cannot prove.

---

# 6. Traps already paid for — general, apply regardless of which location is chosen

1. **Census columns are `humans | robots | combined` at split-indices 4, 5, 6.** Index 3 is Subnet. An
   off-by-one returns **plausible, sensible, wrong** numbers **without erroring.** **Hand-check one printed row
   against the file.**
2. **Census I and II are BOTH pre-war.** The drop between them is **orbital emigration, not war loss.**
   Retention = Census II / Census I. **A population drop between census snapshots is not automatically
   `Declining`** — check whether it is migration to a documented destination first (`01` §3's standing note,
   added after a real self-caught error).
3. **Read symbols from their own registered system file, never from the name.** A registered symbol's meaning
   is whatever its file says, not what the name means in a tradition you happen to know — this project's own
   registered systems (`Planetary_Symbols.md`, `Robot_Elementals.md`) actively diverge from familiar
   associations in places, and reading from the name instead of the file has inverted a capability reading
   before.
4. **Score every quantitative claim as a z-score against the full comparison set, never against a local
   group.** A gap that looks striking against two or three neighbors is frequently unremarkable against the
   whole set. *(A human-vs-robot retention gap has been independently found and killed on the same basis
   twice already, on a different location than whichever one this run is about — expect the same discipline
   to matter here, not necessarily the same specific gap.)*
5. **The universe repo is OUTSIDE this repo** —
   `/home/kuroskalacs/Documents/Doll-Fi/media/Reference/TepenianUniverseTimeline/`. ⚠ **And "repo-wide" does
   not mean canon-wide**: a search that never leaves this repo is evidence about one directory, not about
   canon. **Gate C requires you to name your search paths.**
6. **Verify before claiming novelty**, and **run a proof-of-hit before trusting any zero** — a scan that has
   never been shown to find a real hit has not been shown to find anything.
7. **⭐ Divide population by extent, at Phase 0.** One division, no interpretation. **It is the only
   plausibility check that does not run on the same faculty that produced the error** — Gate 11's only
   recorded catches have come from this, not from its interpretive half.
8. **American English throughout — and this needs an actual mechanical sweep, not just intent.** A rule
   requiring per-sentence vigilance is a rule self-audit is bad at catching in its own prose. **Run a grep
   sweep for common British spellings (colour, favour, humour, behaviour, centre, organise/-d, realise,
   analyse, travelling, modelling, labelled, programme, defence, licence, practise, pretence, grey, amongst,
   whilst, learnt, towards, neighbour, and "sport" where "sports" is meant) before considering any pass
   finished** — this rule has been found broken, undetected, in the methodology's own governing rule text more
   than once, not only in fresh drafting.

---

# 7. Standing canon facts, true regardless of which location is chosen

- **No single nation caused the War of Upper Earth.** It was a global war between humans and robots — and the
  humans who sided with robots — with multiple combatant nations and a complex beginning. Any given nation may
  have persecuted robots and fought in it without having caused it.
- **The Second Interwar Period is 2564–2812, "roughly two and a half centuries."** A "130 years" or "five
  generations" figure for this span is the specific stale constant already found and corrected repo-wide once
  (M-20) — if a figure like this recurs anywhere, check it at the source (the universe repo's own era
  timeline), not at a neighboring file that happens to agree with it.
- **Any location-specific canon** (a particular city's settled facts, reserved decisions, or naming status)
  lives in that location's own Specs/Local_Cultures/research-log files, not in this handoff document. **This
  file deliberately does not carry location-specific canon**, so it does not go stale the moment a different
  location is chosen for the next run. Check the location's own files directly, per §3 above.

---

# 8. What "done" means

**Not "all the boxes have content."** `03`: *a phase that is covered is not a phase that is done.*

> **Could a person live an entire life here — and would that life be unlike a life in any comparable place?**

**If the honest answer is "probably, I suppose," the pass is not finished regardless of what the gates say.
There is no deadline. Go back and make it true.**

> ## ⚠ And for Run 9 specifically, "done" has a second half.
>
> **A finished Janbogo is HALF the deliverable.** The other half is `NN_Ordering_Collision_Log.md` (§2b) —
> written as the run happens, not reconstructed at the end — plus:
>
> - **Every close-order collision encountered**, docketed or not, with which item needed which phase.
> - **Any collision NOT among `03` §0.4's four rows, flagged loudly.** That is the actual finding.
> - **Near-misses** — every point you wanted to defer something and had to consult the docket to decide how.
> - **The negative result, stated explicitly if that is what happened**, naming what you watched for.
> - **The §7 ratification block filled in** (`05` §6.3), listing what was demoted and which findings came out
>   REQUESTED because they rested only on unratified material.
>
> **A Run 9 that hands back a beautiful city and an empty log has not finished.**
