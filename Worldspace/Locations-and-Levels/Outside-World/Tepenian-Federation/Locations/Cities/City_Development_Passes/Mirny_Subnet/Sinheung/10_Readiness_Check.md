# Step 10 — THE READINESS CHECK

**Location:** Sinheung · **Run:** 2026-09-07 · **Next subject checked against:** the remaining Mirny cities

> # ⛔ THE GOVERNING PRINCIPLE: **VERIFY, DO NOT ASSERT.**
> ***"Everything is ready" is a claim, and it requires evidence like any other.*** **Every row below is a
> command that was run, not a judgment that was made.**

---

# 10.1 · CONTAMINATION SURFACE — *run against the NEXT pass's subject*

> ## ⚠ SCOPE RULING FIRST, BECAUSE IT CHANGES WHAT APPLIES
> **All 38 cities are WARM by standing ruling.** **`Run_Modes_Warm_and_Cold.md`:** *WARM = **"the memory
> blackout, reader briefs, pre-contamination review and pins are NOT required."*** ⭐ **So items 1, 3 and 4
> below are N/A BY MODE, not skipped** — *and saying which is the difference between a scoped check and an
> unrun one.*

| # | Check | Result |
|--:|---|---|
| **1** | **Scan auto-loaded memory for the next subject** | ⏸️ **N/A — WARM mode.** *The blackout is a cold-run instrument* |
| **1a** | ⭐ **Does the REQUIRED READING name the next subject?** | ✅ **RUN.** **`Zhongshan` `0` · `Mirny` `0` · `Casey` `0`** across `01`–`05` + `README`. ⚠ **`Davis` returns `2`, and both are `Davis_Geosciences_Research/`** — *a real-world research folder named for the physical station, registered in `Phase 3`'s `MUST OPEN` block.* ⭐ **A `G7` address, not a conclusion about the Tepenian city** |
| **1b** | **Sanitize the file tree** | ⏸️ **N/A — WARM mode.** *No sanitized-path handoff is required* |
| **1c** | ⭐ **Grep this pass's own new prose for the subject's name — SHAPE or CONTENT?** | ✅ **RUN.** *The pass names its own subject freely, which is correct for a warm run; **the methodology files it edited carry `0` new location names** — verified by the LAYERING LAW's own test* |
| **2** | **Update `06_Worked_Example_Provenance.md`** | ✅ **This city already appears `11` times.** ⚠ **This pass added NO new worked examples to `01`–`05`** — *its methodology edits are addresses and rules, not location instances* |
| **3** | **Quarantine list built by RULE, not recall** | ⏸️ **N/A — WARM mode** |
| **4** | **Open every admissible file's header for its own cited sources** | ✅ **RUN, and it FIRED** — *`Step 5` found the culture datasheet declares no `locked-canon` banner and carries four dated self-corrections* → **tiered `3 · DERIVED`, which is what let the pass keep its own findings** |
| **5** | **Corpus-wide retrieval layer flagged as unusable?** | ✅ **`CLAUDE.md` carries the graphify carve-out in `13` places.** ⏸️ *Binding on cold runs only; this was warm and used graphify normally* |

---

# 10.2 · PATH AND STRUCTURE INTEGRITY

| # | Check | Result |
|--:|---|---|
| **6** | ⭐ **Every path named in the handoff exists** | ✅ **RUN MECHANICALLY: `11` absolute addresses in the pass, `11` resolve, `0` broken.** ⭐ **And `55` in `03_The_Phase_Spine.md` + `64` across the 29 step/gate cards, all resolving** |
| **7** | ⚠ **The next run's output folder does NOT already exist** | ⛔ **FIRES ON ITS LITERAL TERMS — `Zhongshan`, `Davis`, `Mirny` and `Casey` folders all exist.** ⭐⭐ **AND IS A FALSE POSITIVE ON ITS OWN RATIONALE:** *each holds exactly one `README.md`, `44` lines, `3` headings, identical in structure across all five including this city's* — **a name-substituted template.** ***The rule's stated reasons are "a contamination risk and a status lie"; a template with no findings and no completion claim is neither.*** ⏸️ **Recorded as a rule/rationale mismatch worth a ruling, not as a blocker** |
| **8** | **Observations file OUTSIDE every quarantined folder** | ✅ **VERIFIED** — *it is a sibling of the run folders, so a future quarantined run can still write to it* `(M-0)` |

---

# 10.3 · RECORD INTEGRITY

| # | Check | Result |
|--:|---|---|
| **9** | ⭐ **Gate 0 outward — does every completion claim match the files?** | ⛔ **FIRED.** *This city's own `README.md` still said **"Empty by design"** with 17 files in the folder, and predicted a layout the pass diverged from* — **a `06_Differentiation.md` that correctly does not exist, one file per gate rather than one file for all 17, and no mention of `09.6_Input_Audit.md`.* ✅ **Rewritten to LIST what the folder contains.** ⭐ **Its relative paths were converted to absolute in the same edit** |
| **10** | ⭐⭐ **Findings IMPLEMENTED, not merely recorded** | ⛔ **FIRED.** *`M-170` was cited in **`0`** methodology files beyond the log* — **a finding logged and absent from the rule it should have changed has not been made.** ✅ **Now implemented into `§B`'s standing rule as a third consequence.** ⏸️ **`M-168` remains at `1` DELIBERATELY — its fix is proposed and awaiting a developer ruling** |
| **11** | ⭐ **Does the handoff state what the run CANNOT prove?** | ✅ **`09_Record.md` §6 states five limits explicitly**, including that a WARM run's convergences are not the cold-run evidence class |

---

# ▶ WHAT THIS CHECK CAUGHT — **three fires, and the check earned its place again**

| | |
|---|---|
| ⛔ **Item 9** | **The pass's own README claimed "empty by design" while holding 17 files** — *Gate 0's exact shape, in the pass's own folder* |
| ⛔ **Item 10** | **A methodology finding recorded but never implemented into the rule it changed** |
| ⚠ **Item 7** | **A rule firing correctly on its terms and wrongly on its reasons** — *worth a ruling, because the next 36 cities will trip it identically* |

---

# ▶ READINESS VERDICT

> ## ✅ **THE `Sinheung` ULM PASS IS COMPLETE — Steps −1 through 10.**
> **Not asserted: `17` files exist and are listed in `09_Record.md` §1; `15` of `17` gates ran with raw output
> pasted; all six Review Panel dispositions were used; both differentiation tables carry this city's entry; and
> every path in the pass resolves mechanically.**

| ⏸️ HANDED TO THE NEXT PASS | |
|---|---|
| ⭐ **The next city geographically** | **Zhongshan** *(Mirny, Larsemann Hills)* — ✅ **`0` mentions in required reading** |
| ⛔ **`DRQ-11` blocks all three Larsemann cities** | *the rock allocation must be answered ONCE for the cluster* |
| ⛔ **`DRQ-17`'s national half is unresolved** | *every other city's industry headcounts are stale by the same `−10%`* |
| ⏸️ **`M-168`'s fix awaits a ruling** | *until then, every pass must sweep inbound handoffs by hand* |
| ⭐ **`16` unopened `§C.8c` targets remain docketed** | `09.6_Input_Audit.md` |

📎 **The pass is closed. `CST` and `RWBEM` run later, breadth-first, after all 38 ULM passes.**
